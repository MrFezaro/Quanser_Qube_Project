controller_manager:
  ros__parameters:
    update_rate: 100

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

    velocity_controller:
      type: velocity_controllers/JointGroupVelocityController  # ← fixed

velocity_controller:
  ros__parameters:
    joints:           # ← note: plural for JointGroup
      - motor_joint
    interface_name: velocity